# Portfolio Application

The active application is the static Next.js site in `frontend`. It is built for GitHub Pages and does not require a backend service.

```bash
cd frontend
npm ci
npm run lint
npm run build
```

Edit experience, projects, skills, and public links in `frontend/utils/constants.ts`. See the repository-root README for structure, evidence rules, and deployment details.
